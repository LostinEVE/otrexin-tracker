<% content_for :title, "New Settlement Template" %>

<div class="page-header">
  <div class="page-title-group">
    <h1>New Settlement Template</h1>
    <p class="page-kicker">Enter the deduction lines once. After this, a whole settlement takes one form.</p>
  </div>
</div>

<%= render "form", settlement_template: @settlement_template %>
